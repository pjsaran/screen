<Page
    x:Class="Captr.App.Views.DeliveryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Delivery">
    <DockPanel Margin="24">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Delivery" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center" />
            <ui:Button Content="Refresh" Margin="16,0,0,0" Command="{Binding RefreshCommand}" />
            <TextBlock Text="{Binding Message}" Margin="16,0,0,0" VerticalAlignment="Center" Opacity="0.8" />
        </StackPanel>
        <ItemsControl ItemsSource="{Binding Deliveries}">
            <ItemsControl.Template>
                <ControlTemplate TargetType="ItemsControl">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsPresenter />
                    </ScrollViewer>
                </ControlTemplate>
            </ItemsControl.Template>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,0,8" Padding="12" CornerRadius="8"
                            Background="{DynamicResource ControlFillColorDefaultBrush}">
                        <StackPanel>
                            <TextBlock FontWeight="SemiBold">
                                <Run Text="{Binding File, Mode=OneWay}" /> →
                                <Run Text="{Binding Destination, Mode=OneWay}" />
                            </TextBlock>
                            <TextBlock Opacity="0.8" Margin="0,2,0,0">
                                <Run Text="{Binding State, Mode=OneWay}" /> · attempts:
                                <Run Text="{Binding Attempts, Mode=OneWay}" />
                            </TextBlock>
                            <TextBlock Text="{Binding Explanation}" Margin="0,4,0,0" TextWrapping="Wrap" />
                            <TextBlock Text="{Binding ServerError}" Margin="0,4,0,0" TextWrapping="Wrap"
                                       FontFamily="Consolas" FontSize="12" Opacity="0.7" />
                            <ui:Button Content="Retry" FontSize="12" Margin="0,8,0,0" HorizontalAlignment="Left"
                                       IsEnabled="{Binding CanRetry}"
                                       Command="{Binding DataContext.RetryCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                       CommandParameter="{Binding}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</Page>
