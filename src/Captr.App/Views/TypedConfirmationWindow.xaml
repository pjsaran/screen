<Window
    x:Class="Captr.App.Views.TypedConfirmationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Confirm"
    Width="460" SizeToContent="Height" WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize" ShowInTaskbar="False">
    <StackPanel Margin="20">
        <TextBlock x:Name="TitleText" FontSize="18" FontWeight="SemiBold" />
        <TextBlock x:Name="ExplanationText" Margin="0,10,0,0" TextWrapping="Wrap" />
        <TextBox x:Name="ConfirmationBox" Margin="0,14,0,0" TextChanged="OnTextChanged" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" MinWidth="90" IsCancel="True" />
            <Button x:Name="ConfirmButton" Content="Delete" MinWidth="90" Margin="8,0,0,0"
                    IsEnabled="False" Click="OnConfirmClicked" />
        </StackPanel>
    </StackPanel>
</Window>
