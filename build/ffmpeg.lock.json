{
  "//": [
    "Pin for the bundled FFmpeg build (SPEC §2/§11: fetch a pinned encoder build,",
    "verify against a committed checksum, record the exact upstream build identifier).",
    "This pins the GPL variant DELIBERATELY — a recorded decision, not a mistake:",
    "this deployment is internal-only (nothing is conveyed outside the organisation),",
    "and the GPL build carries libx264, whose true CRF mode gives the software",
    "fallback constant-quality encoding on machines without a working GPU encoder",
    "(see docs/developer-guide/design-decisions.md). If this app is ever distributed",
    "outside the organisation, repoint at the -lgpl asset of the same tag and drop",
    "libx264 from optionalEncoders — the encoder trial system degrades gracefully.",
    "Never pin the rolling 'latest' tag.",
    "ONLY PIN A MONTH-END TAG (the last autobuild of a month, e.g. autobuild-2026-08-31-*).",
    "BtbN/FFmpeg-Builds deletes daily autobuilds after roughly two weeks and keeps only",
    "the month-end one. A mid-month pin works on the machine that fetched it (the zip is",
    "cached in tools/ffmpeg) and then 404s on every other machine and in CI once upstream",
    "prunes it — which is exactly what happened with autobuild-2026-08-18-15-03.",
    "To upgrade: pick a MONTH-END autobuild tag from github.com/BtbN/FFmpeg-Builds/releases,",
    "copy the asset's line from its checksums.sha256, update this file, and re-run",
    "build/fetch-ffmpeg.ps1 — it fails hard on any mismatch. Then run",
    "build/check-licenses.ps1 -Update so THIRD-PARTY.md records the new build id."
  ],
  "repository": "BtbN/FFmpeg-Builds",
  "releaseTag": "autobuild-2026-08-31-13-27",
  "assetName": "ffmpeg-n8.1.2-50-g1a748fe2cd-win64-gpl-8.1.zip",
  "sha256": "273abb45f3f9f76c303e35ff39f5bb6c23c163ae65f6244a32b7d4a7f6cf0616",
  "buildId": "ffmpeg-n8.1.2-50-g1a748fe2cd-win64-gpl",
  "licenceFlavor": "gpl",
  "requiredFilters": ["ddagrab", "hwdownload", "pad", "hstack", "xstack", "drawtext"],
  "requiredEncoders": ["hevc_nvenc", "h264_nvenc", "hevc_qsv", "h264_qsv", "hevc_amf", "h264_amf"],
  "optionalEncoders": ["libx264", "libopenh264"],
  "requiredMuxers": ["segment", "matroska"]
}
